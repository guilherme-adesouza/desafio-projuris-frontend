import { environment } from 'src/environments/environment';

export class ApiConstants {
    public static API_ENDPOINT = environment.API_ENDPOINT;
    public static API_MEDIA_ENDPOINT = environment.API_MEDIA_ENDPOINT;
}